gsap.fromTo(".IwolfU",
    { opacity: 0, scale: 0 },
    { opacity: 1, scale: 1, duration: 1.15 }
  );

  gsap.fromTo(".card-loader",
  { opacity: 0, scale: 0 },
  { opacity: 1, scale: 1, duration: 1.15 }
);